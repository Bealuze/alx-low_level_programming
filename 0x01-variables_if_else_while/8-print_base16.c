#include <stdio.h>
/**
 * main-entry
 * Return: always 0
 */

int main(void)
{
	int x;
	char a;

	for (x = 0; x <= 9; x++)
		putchar(int x);
	for (a = 'a'; a <= 'f'; a++)
		putchar(a);

	return (0);
}
